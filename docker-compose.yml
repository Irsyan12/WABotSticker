services:
  stickerbot:
    build: .
    container_name: stickerwhatsappbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
